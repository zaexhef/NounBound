# NounBound environment example — never commit real secrets.

APP_ENV=development
EAS_PROJECT_ID=replace-with-eas-project-id

# Feature flags (production defaults remain off until owner approval)
ENABLE_ECONOMY=false
ENABLE_REWARDED_ADS=false
ENABLE_PURCHASES=false

# Store / EAS (fill locally or in EAS secrets — do not commit values)
# ASC_APP_ID=
# APPLE_ID=
# APPLE_TEAM_ID=
# GOOGLE_SERVICE_ACCOUNT_KEY=
